services:
  postgres:
    image: postgres:15
    container_name: DIVS-postgres
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  flask-server:
    build: ./Flask
    container_name: DIVS-flask
    restart: always
    depends_on:
      - postgres
    env_file: .env

  nginx:
    image: nginx:latest
    container_name: DIVS-nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - flask-server

volumes:
  postgres_data: